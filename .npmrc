# Build configuration
build-from-source=false

# Python configuration (required for native modules)
# Uncomment and set if you have Python installed in a custom location
# python=/path/to/python3

# Electron configuration
electron_mirror=https://cdn.npmmirror.com/binaries/electron/
electron_builder_binaries_mirror=https://cdn.npmmirror.com/binaries/electron-builder-binaries/

# Better SQLite3 configuration
better_sqlite3_binary_host=https://github.com/WiseLibs/better-sqlite3/releases/download/
